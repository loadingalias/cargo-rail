[package]
name = "cargo-rail-compat-wasm"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
cargo-rail-compat-wasm-support = { path = "support" }

[features]
default = []
variant = []

[lints.rust]
unsafe_code = "forbid"

[workspace]
members = ["support"]
