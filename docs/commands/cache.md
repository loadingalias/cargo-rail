# cargo-rail cache

<!-- Generated by just docs; edit CLI help in src/, then regenerate. -->

[Command reference](README.md)

```text
Install, inspect, or reclaim explicitly scoped compiler-cache state

Usage: cargo-rail cache [OPTIONS] <COMMAND>

Commands:
  setup         Install or repair transparent verified compiler reuse
  report        Start or finish one explicit cache measurement interval
  normalize     Validate and normalize a remote cache URL without network access
  probe         Authenticate the selected remote object store and validate its protocol marker
  status        Report cache installation and owned-storage health
  profiles      Inspect every private workspace profile without selecting or mutating one
  detach        Detach the current workspace while preserving its profile and CAS data
  drop-profile  Permanently remove one detached workspace profile and its owned local state
  recover       Quarantine a selected markerless CAS and create a fresh owned authority
  clean         Reclaim one explicitly selected cache scope
  uninstall     Uninstall the one global wrapper while preserving every workspace profile and CAS
  help          Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)

Cache setup enrolls this workspace for ordinary Cargo commands.
Remote storage and distributed workers require explicit machine-owned authority;
repository policy cannot enable them.

  cargo rail cache setup --check               # Preview enrollment
  cargo rail cache setup                       # Install or repair the wrapper
  cargo rail cache clean --scope local --check # Preview selected-profile cleanup

Detach preserves the profile and CAS. Uninstall removes the global wrapper while
preserving all profiles and their CAS data.
```

## cargo-rail cache setup

```text
Install or repair transparent verified compiler reuse

Usage: cargo-rail cache setup [OPTIONS]

Options:
      --profile <PROFILE_ID>
          Existing opaque profile ID to bind this workspace explicitly

      --local-dir <PATH>
          Local cache base directory (defaults to Cargo home)

      --max-size <SIZE>
          Maximum local cache size; use a positive integer with B, KiB, MiB, GiB, or TiB

      --remote <URL>
          Machine-owned remote cache URL to persist with this workspace profile

      --remote-mode <MODE>
          Maximum remote authority; explicit selection defaults to read-write

          [possible values: read, read-write]

      --remote-environment <NAME>
          Additional reviewed compiler environment name admitted to L2 identity

      --root-portability <MODE>
          Root identity mode: physical binds exact paths; remap permits eligible cross-root reuse

          [possible values: physical, remap]

      --local-only
          Remove persisted remote activation while preserving local reuse

      --distributed-local
          Enable the same-host distributed protocol qualification path

      --distributed-endpoint <IP:PORT>
          Mutually authenticated direct worker socket address

      --distributed-server-name <NAME>
          TLS DNS name required from the distributed worker certificate

      --distributed-capability <IDENTITY>
          Exact capability identity advertised by the selected worker

      --distributed-authority <PATH>
          PEM certificate authority for the distributed worker

      --distributed-client-certificate <PATH>
          PEM client certificate presented to the distributed worker

      --distributed-client-private-key <PATH>
          Private PEM key for the distributed client certificate

      --distributed-policy <MODE>
          Placement policy for an mTLS worker. Qualification samples every eligible miss; automatic placement requires retained evidence of a critical-path win

          [possible values: automatic, qualification]

  -c, --check
          Preview exact Cargo configuration and private-state changes

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache report

```text
Start or finish one explicit cache measurement interval

Usage: cargo-rail cache report [OPTIONS]

Options:
      --start <START>
          Create a new private recording; export CARGO_RAIL_CACHE_REPORT to this absolute path

      --finish <FINISH>
          Finish the interval and report aggregated compiler outcomes

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache normalize

```text
Validate and normalize a remote cache URL without network access

Usage: cargo-rail cache normalize [OPTIONS] <URL>

Arguments:
  <URL>
          AWS S3, Azure Blob Storage, or Cloudflare R2 URL

Options:
      --mode <MODE>
          Maximum authority; explicit selection defaults to read-write

          [possible values: read, read-write]

      --environment <NAME>
          Additional reviewed compiler environment name admitted to L2 identity

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache probe

```text
Authenticate the selected remote object store and validate its protocol marker

Usage: cargo-rail cache probe [OPTIONS]

Options:
  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache status

```text
Report cache installation and owned-storage health

Usage: cargo-rail cache status [OPTIONS]

Options:
      --scope <SCOPE>
          Cache scope to inspect

          Possible values:
          - workspace: Reconstructible cache state inside the selected workspace
          - local:     The validated local CAS owned by the selected workspace profile
          - all:       Both workspace state and the selected profile's local CAS

          [default: all]

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache profiles

```text
Inspect every private workspace profile without selecting or mutating one

Usage: cargo-rail cache profiles [OPTIONS]

Options:
  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache detach

```text
Detach the current workspace while preserving its profile and CAS data

Usage: cargo-rail cache detach [OPTIONS]

Options:
  -c, --check
          Preview the exact binding removal without modifying profile state

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache drop-profile

```text
Permanently remove one detached workspace profile and its owned local state

Usage: cargo-rail cache drop-profile [OPTIONS] --profile <PROFILE>

Options:
      --profile <PROFILE>
          Opaque profile identifier reported by `cargo rail cache profiles`

  -c, --check
          Preview the exact profile removal without modifying cache state

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache recover

```text
Quarantine a selected markerless CAS and create a fresh owned authority

Usage: cargo-rail cache recover [OPTIONS]

Options:
  -c, --check
          Preview the exact quarantine move without modifying cache state

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache clean

```text
Reclaim one explicitly selected cache scope

Usage: cargo-rail cache clean [OPTIONS] --scope <SCOPE>

Options:
      --scope <SCOPE>
          Cache scope to reclaim; required to prevent accidental cross-workspace deletion

          Possible values:
          - workspace: Reconstructible cache state inside the selected workspace
          - local:     The validated local CAS owned by the selected workspace profile
          - all:       Both workspace state and the selected profile's local CAS

  -c, --check
          Preview exact bytes and paths without deleting them

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```

## cargo-rail cache uninstall

```text
Uninstall the one global wrapper while preserving every workspace profile and CAS

Usage: cargo-rail cache uninstall [OPTIONS]

Options:
  -c, --check
          Preview exact Cargo configuration and private-state changes

  -f, --format <FORMAT>
          Report format

          Possible values:
          - text: Human-readable text
          - json: JSON

          [default: text]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global Options:
  -q, --quiet
          Suppress progress messages

  -v, --verbose
          Show additional operational detail

      --json
          Output as JSON where supported; rejected otherwise

      --config <PATH>
          Path to rail.toml config file (bypass search order)

      --workspace-root <PATH>
          Workspace root directory (default: current directory)
```
