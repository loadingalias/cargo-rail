---
"cargo-rail" = "minor"
---

Read supported older configuration automatically without modifying files or requiring upgrade steps. Bare `cargo rail config` explains active policy; inspection validates semantic policy, reports missing workspace context, and preserves target inheritance in canonical exports. Remove the configuration migration command and its platform-specific writer while preserving release and cache authority.
