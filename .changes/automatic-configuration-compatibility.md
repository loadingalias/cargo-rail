---
"cargo-rail" = "minor"
---

Bare `cargo rail config` explains active policy. Inspection validates semantic policy, reports missing workspace context, and preserves target inheritance in canonical exports. Configuration accepts only current fields and never rewrites input. Explanation schema 2 removes compatibility provenance.
