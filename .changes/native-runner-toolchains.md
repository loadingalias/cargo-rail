---
"cargo-rail" = "patch"
---

Observe GCC-selected Rust LLD inputs and loader-verified process-image replacements for native compiler-cache reuse. Normalize Windows executable paths consistently for compiler and wrapper verification. Validate provisioned toolchains with native compilation and execution, preserve MSVC linker precedence, and use the shared nextest policy on native test runners.
