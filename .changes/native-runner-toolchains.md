---
"cargo-rail" = "patch"
---

Observe GCC-selected Rust LLD inputs and loader-verified process-image replacements for native compiler-cache reuse. Recognize rustc's GNU and Darwin LLD target flavors that use a C compiler driver. Normalize Windows executable paths consistently for compiler and wrapper verification. Validate provisioned toolchains with native compilation and execution, preserve MSVC linker precedence, and use the shared nextest policy on native test runners.
