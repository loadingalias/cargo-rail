---
"cargo-rail" = "patch"
---

Declare glibc 2.35 as the GNU Linux archive floor, build and exercise every GNU executable on that runtime, reject newer symbol requirements, and make the installer refuse unsupported hosts before replacing an existing installation.
