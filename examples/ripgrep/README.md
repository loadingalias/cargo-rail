# ripgrep + cargo-rail

**Repo:** <https://github.com/BurntSushi/ripgrep>
**Commit:** `cd1f981` (2025-11-29)

## Demo

![ripgrep demo](./demo.gif)

## Commands

```bash
# Clone and enter
git clone https://github.com/BurntSushi/ripgrep
cd ripgrep

# Initialize cargo-rail
cargo rail init

# Check and apply unification
cargo rail unify --check
cargo rail unify
```

## Impact Summary

| Metric | Value |
|--------|-------|
| Workspace members | **10** (not 1!) |
| Dependencies unified | 9 |
| Member edits | 35 |
| Transitives pinned | 5 |

**Dependencies unified:** serde_json, regex, serde, bstr, memchr, termcolor, globset, walkdir, log

## Configuration

```toml
# .config/rail.toml (generated by cargo rail init)
[unify]
include_paths = true
include_renamed = false
pin_transitives = true
transitive_host = "root"
exclude = []
include = []
msrv = false
strict_version_compat = true
exact_pin_handling = "warn"
detect_unused = false
```

## Notes

- **Surprise:** ripgrep is NOT a single crate - it has 10 workspace members!
- Popular grep replacement tool
- Demonstrates that even "simple" tools can benefit from unification
- Good example of discovering hidden workspace complexity

Details in [`summary.toml`](./summary.toml).
