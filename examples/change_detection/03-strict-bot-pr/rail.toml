# cargo-rail configuration
# Strict bot PR policy with complete [change-detection] and [run] sections.

[change-detection]
# Paths that trigger full workspace rebuild
infrastructure = [
  ".github/**",
  "scripts/**",
  "justfile",
  "Justfile",
  "Makefile",
  "makefile",
  "GNUmakefile",
  "*.sh",
  "Taskfile.yml",
  "Taskfile.yaml",
  ".pre-commit-config.yaml",
  "deny.toml",
  "cliff.toml",
  "release.toml",
  "release-plz.toml",
]
conservative_unclassified_owner_fallback = true
confidence_profile = "balanced"
bot_pr_confidence_profile = "strict"

[run]
default_profile = "local"

[run.profile.local]
surfaces = ["test"]
run_args = []
merge_base = true

[run.profile.ci]
surfaces = ["build", "test"]
run_args = ["--workspace", "{cargo_args}"]
merge_base = true

[run.profile.docs]
surfaces = ["docs"]
run_args = []
merge_base = true
