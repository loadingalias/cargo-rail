# Complete single-crate split policy.

[crates.cargo-rail.split]
remote = "git@github.com:your-org/cargo-rail-split.git"
branch = "main"
mode = "single" # single | combined
workspace_mode = "standalone" # standalone | workspace
members = ["cargo-rail"]
include = ["LICENSE", "README.md"]
exclude = ["README.md"]
